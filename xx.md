# 阵列信号处理学习笔记（第一阶段：基础理论）

> 学习目标：建立从电磁波传播到现代阵列信号处理（MUSIC、MVDR、CRPA）的完整知识体系。

---

# 整体知识框架

```text
                    电磁波传播
                         │
                         ▼
                  平面波（远场假设）
                         │
                         ▼
                    路径差 Δl
                         │
                         ▼
                    时间差 Δt
                         │
                         ▼
                    相位差 Δφ
                         │
                         ▼
                    导向矢量 a(θ)
                         │
          ┌──────────────┴──────────────┐
          ▼                             ▼
      Beamforming                 阵列因子(AF)
          │                             │
          ▼                             ▼
      波束扫描                  主瓣/旁瓣/栅瓣
          │                             │
          └──────────────┬──────────────┘
                         ▼
                 接收信号模型 x(t)
                         │
                         ▼
                  Snapshot（快拍）
                         │
                         ▼
                  协方差矩阵 R
                         │
                         ▼
                   特征值分解(EVD)
                         │
           ┌─────────────┴─────────────┐
           ▼                           ▼
      信号子空间                  噪声子空间
                         │
                         ▼
                MUSIC / ESPRIT / MVDR
```

---

# 第一章 阵列几何

## 1. 阵元（Element）

阵元是阵列中能够独立接收或发射信号的基本单元。
在典型接收阵列中，每个阵元通常对应：

- 一个物理天线单元；
- 一条射频前端链路；
- 一路 ADC 或数字中频数据；
- 一个随时间变化的复数采样序列。


对于 N 阵元阵列，在第 k 个采样时刻得到的接收向量为：

$$
\mathbf{x}[k]
=
\begin{bmatrix}
x_1[k] \\
x_2[k] \\
\vdots \\
x_N[k]
\end{bmatrix}
$$

这里每个 `x_i[k]` 是一个复数采样值，不是整段时间序列。

例如四阵元：

```
O   O   O   O
```

第 i 个阵元的完整时间序列则是：

$$
x_i[0],\;x_i[1],\;x_i[2],\;\ldots
$$

---

## 2. 阵列类型

按照阵元在空间中的排布方式，可以分为：

- 线性阵列：阵元沿一条直线排列；
- 平面阵列：阵元分布在一个平面内；
- 圆阵：阵元沿圆周排列；
- 立体阵列：阵元分布在三维空间中；
- 共形阵列：阵元贴合在曲面载体上。

线性阵列通常只能利用一个空间方向上的相位变化。平面阵列可以同时利用两个方向上的空间相位变化，因此能够估计方位角和俯仰角。

---

## 3. 阵元间距

均匀线阵中，相邻阵元间距通常记为 d。

为了避免扫描区域内出现空间混叠，常采用：

$$
d\leq \frac{\lambda}{2}
$$

其中 lambda 是载波波长。

当阵元间距过大时，不同入射方向可能产生相同的阵元间相位差，从而出现栅瓣。

---

## 4. 阵列孔径（Aperture）

对于 N 阵元均匀线阵，如果相邻阵元间距为 d，则首尾阵元中心之间的几何孔径为：

$$
L=(N-1)d
$$

孔径主要决定主瓣宽度和角度分辨能力。

需要区分：

- 固定阵元间距时，增加阵元数会同时增大孔径，所以主瓣通常变窄；
- 固定孔径时，增加阵元数主要提高空间采样密度、旁瓣控制能力和自由度；
- 对阵元数较少的离散阵列，即使孔径相同，主瓣宽度也可能存在一定差别；
- 当阵元足够密集时，离散阵列逐渐接近连续孔径模型。
核心理解：

> 决定角分辨率的是孔径，而不是阵元数量。**对于离散阵列，主瓣宽度主要由有效孔径决定，但当阵元数较少时，离散采样效应也会影响方向图；随着阵元数增加、阵元间距减小，离散阵列逐渐逼近连续孔径，此时主瓣宽度主要由孔径决定。**

---

# 第二章 电磁波传播

## 5. 波速、频率和波长

波速、频率与波长的关系为：

$$
v=f\lambda
$$

因此：

$$
\lambda=\frac{v}{f}
$$

GPS L1 载波频率为 1575.42 MHz，在真空中的波长约为：

$$
\lambda_{\mathrm{L1}}
=
\frac{299792458}{1575.42\times 10^6}
\approx 0.1903\;\mathrm{m}
$$

即约 19.03 cm。

## 6. 波数

波数表示波每传播单位距离所积累的相位：

$$
k=\frac{2\pi}{\lambda}
$$

其单位通常为 rad/m。

因此，传播距离差与相位差之间满足：

$$
\Delta\phi=k\Delta l
$$

---
## 5. 平面波假设

远场条件：

球面波

↓

近似为

平面波

因此：

整个阵列接收到的是平行波前。

---

## 7. 远场和平面波近似

严格地说，点源发出的波是球面波。对于阵列中第 n 个阵元，路径差应由各阵元到信号源的实际距离计算。

当信号源距离远大于阵列孔径时，阵列局部看到的球面波可以近似为平面波。

常见远场判据为：

$$
R\gg \frac{2D^2}{\lambda}
$$

其中 D 是阵列最大尺寸。

GNSS 卫星距离远大于普通接收阵列尺寸，因此平面波近似通常非常准确。


## 6. 路径差
对于阵元坐标沿 x 轴排列的均匀线阵，设入射角定义使正侧向为 0 度，则第 n 个阵元相对于参考阵元的远场路径差可写为：

$$
\Delta l_n=nd\sin\theta
$$

角度定义不同，公式中也可能出现 cos。关键是必须明确角度相对于阵列轴还是阵列法线定义。

---

## 7. 时间差与相位差

路径差对应的传播时间差为：

$$
\Delta\tau_n=\frac{\Delta l_n}{c}
$$

相位差为：

$$
\Delta\phi_n
=
2\pi f_c\Delta\tau_n
$$

利用波长关系，可以写成：

$$
\Delta\phi_n
=
knd\sin\theta
$$

阵列真正利用的是：

> **不同阵元之间的载波相位差。**

---

## 9. 波数（Wave Number）

定义：

\[
k=\frac{2\pi}{\lambda}
\]

物理意义：

> 电磁波传播一米，相位变化多少弧度。

因此：

\[
\Delta\phi
=
kd\sin\theta
\]

---

# 第三章 导向矢量（Steering Vector）

## 10. 导向矢量

对于 N 阵元均匀线阵，导向矢量可以写为：

$$
\mathbf{a}(\theta)
=
\begin{bmatrix}
1 \\
e^{-jkd\sin\theta} \\
e^{-j2kd\sin\theta} \\
\vdots \\
e^{-j(N-1)kd\sin\theta}
\end{bmatrix}
$$

导向矢量描述来自某个方向的平面波在各阵元上形成的相对复数幅相关系。

它可以理解为该方向的空间指纹。

---

## 11. 阵列信号模型

单目标：

\[
\boxed{
x=a(\theta)s+n
}
\]

多目标：

\[
\boxed{
x=\sum_i a(\theta_i)s_i+n
}
\]

其中：

- a：空间信息
- s：GNSS信号
- n：噪声

这是现代阵列处理最基础的模型。

---

# 第四章 Beamforming

## 12. Beamforming本质

不是复杂算法。

本质：

> 相位补偿 + 相干叠加。

目的：

使目标方向所有阵元重新同相。

---

## 13. Beamforming公式

\[
\boxed{
y=w^Hx
}
\]

其中：

w 为权值。

对于传统波束形成：

\[
w=a^*(\theta)
\]

---

## 14. 波束扫描

改变：

\[
w
\]

即可改变波束方向。

无需机械转动。

---

# 第五章 阵列方向图

## 15. 阵列因子（Array Factor）

本质：

> 阵列对于不同方向的响应。

数学表达：

\[
AF(\theta)=w^Ha(\theta)
\]

---

## 16. 主瓣（Main Lobe）

目标方向。

输出最大。

---

## 17. 旁瓣（Side Lobe）

目标方向之外仍存在响应。

原因：

有限孔径必然产生旁瓣。

---

## 18. 栅瓣（Grating Lobe）

产生条件：

\[
d>\frac{\lambda}{2}
\]

本质：

空间采样混叠。

---

## 19. 波束宽度

规律：

孔径越大

↓

波束越窄

↓

角分辨率越高。

---

# 第六章 GNSS阵列模型

## 20. 为什么写成 s(t)

真实传播：

\[
s(t-\tau)
\]

由于：

阵元传播延迟只有约

0.3ns

而：

GPS C/A码

977ns

因此：

满足窄带假设：

\[
s(t-\tau)
\approx
s(t)e^{-j2\pi f\tau}
\]

---

## 21. 窄带阵列

阵列真正利用的是：

**载波相位差**

不是：

- C/A码差
- 导航电文差

---

# 第七章 Snapshot（快拍）

## 22. Snapshot

一次ADC采样得到：

\[
x[k]
=
\begin{bmatrix}
x_1[k]\\
x_2[k]\\
\vdots\\
x_N[k]
\end{bmatrix}
\]

注意：

每个

\[
x_i[k]
\]

是一个复数。

不是时间序列。

---

## 23. 数据矩阵

假设：

N阵元

K个Snapshot

数据矩阵：

\[
X
=
N\times K
\]

例如：

```
        Snapshot →

        1 2 3 4 ...

阵元1

阵元2

阵元3
```

---

# 第八章 协方差矩阵

## 24. 协方差矩阵

定义：

\[
\boxed{
R
=
E(xx^H)
}
\]

工程实现：

\[
\boxed{
R
=
\frac1KXX^H
}
\]

意义：

记录所有阵元之间的统计关系。

对角线：

阵元功率。

非对角线：

阵元相关性。

---

# 第九章 子空间理论

## 25. 信号子空间

无噪声时：

若只有一个信号：

所有Snapshot均满足：

\[
x[k]=a(\theta)s[k]
\]

因此：

所有数据均位于：

**同一条直线**

即：

信号子空间。

---

若两个信号：

所有数据位于：

一个二维平面。

---

## 26. 特征值分解（EVD）

协方差矩阵：

\[
R
=
U\Lambda U^H
\]

得到：

- 特征值
- 特征向量

物理意义：

寻找数据变化最主要的方向。

---

## 27. 信号子空间

对应：

最大的几个特征值。

其特征向量张成：

Signal Subspace。

---

## 28. 噪声子空间

对应：

较小特征值。

其特征向量张成：

Noise Subspace。

---

# 第十章 下一阶段学习路线

已经完成：

- ✅ 阵列几何
- ✅ 平面波
- ✅ 路径差
- ✅ 相位差
- ✅ 波数
- ✅ 导向矢量
- ✅ Beamforming
- ✅ 阵列因子
- ✅ 主瓣/旁瓣/栅瓣
- ✅ Snapshot
- ✅ 协方差矩阵
- ✅ 特征值分解
- ✅ 信号子空间
- ✅ 噪声子空间

下一阶段：

1. MUSIC算法（子空间DOA估计）
2. ESPRIT算法
3. MVDR（Capon）波束形成
4. LCMV波束形成
5. CRPA抗干扰
6. STAP（空时自适应处理）
7. 宽带阵列与Galileo E5全带宽处理

---

# 核心思想总结

现代阵列信号处理的完整逻辑链：

```text
电磁波传播
      │
      ▼
平面波
      │
      ▼
路径差 Δl
      │
      ▼
相位差 Δφ
      │
      ▼
导向矢量 a(θ)
      │
      ▼
Beamforming
      │
      ▼
阵列因子 AF
      │
      ▼
方向图
      │
      ▼
Snapshot
      │
      ▼
协方差矩阵 R
      │
      ▼
特征值分解 EVD
      │
      ├────► 信号子空间
      │
      └────► 噪声子空间
      │
      ▼
MUSIC / ESPRIT / MVDR / CRPA
```
